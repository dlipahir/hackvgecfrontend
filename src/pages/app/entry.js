import React from 'react'
import styles from '@/styles/Entry.module.css'
import {getTodayEntries} from '../../utils/firebaseConfig'

const Entry = () => {
  
  return (
    <div className={styles.cont}>Entry</div>
  )
}

export default Entry